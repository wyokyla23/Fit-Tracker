import { createMuiTheme } from "@material-ui/core/styles";

const fitPurple = "#292659";
const fitLightPurple = "#C0BFD1";

export default createMuiTheme({
  palette: {
    common: {
      purple: `${fitPurple}`,
      lightPurple: `${fitLightPurple}`,
    },
  },
});
